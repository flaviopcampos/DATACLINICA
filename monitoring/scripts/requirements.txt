# Dependências para os scripts de automação do sistema de monitoramento DataClinica

# Requisições HTTP
requests>=2.31.0

# Manipulação de JSON
# (json é built-in no Python)

# Manipulação de arquivos e diretórios
# (os, shutil, pathlib são built-in no Python)

# Data e hora
# (datetime é built-in no Python)

# Argumentos de linha de comando
# (argparse é built-in no Python)

# Logging
# (logging é built-in no Python)

# Compressão de arquivos
# (tarfile é built-in no Python)

# Docker (opcional, para verificação avançada de containers)
docker>=6.1.0

# Colorização de output no terminal (opcional)
colorama>=0.4.6

# Formatação de tabelas (opcional)
tabulate>=0.9.0

# Validação de URLs (opcional)
validators>=0.20.0